# 冰美肌预约 - 时段开关 / 防冲突功能上线 Checklist

> 代码改动已完成（跨用户时段冲突 + 操作师控制时段开关）。
> 但云函数和数据库集合需要**手动在微信开发者工具/云开发控制台部署**后，真机才生效。
> 本清单按步骤执行即可。

---

## 一、上传并部署两个云函数（必须）

在 **微信开发者工具** 中打开工程（目录指向 `miniapp-b2c`）：

1. 左侧「云开发」→ 确认已开通云环境（记下你的 **环境 ID**）。
2. 在文件树找到 `cloudfunctions/saveSchedule`：
   - 右键 → **「上传并部署：云端安装依赖」**
   - 等待提示「上传成功 / 部署完成」。
3. 同样对 `cloudfunctions/checkSlot`：
   - 右键 → **「上传并部署：云端安装依赖」**。
4. 验证：云开发控制台 →「云函数」列表应出现 `saveSchedule`、`checkSlot`，且状态正常。

> ⚠️ 若之前已有旧云函数（如 `getAdminBookings`），不影响；只部署这两个新的即可。
> 若改过云函数代码需要重新生效，重复上述"上传并部署"步骤。

---

## 二、新建 `schedule` 集合 + 确认索引（必须）

进入 **云开发控制台 → 数据库**：

1. **新建集合 `schedule`**
   - 点击「新建集合」→ 名称填 `schedule` → 确定。
   - 权限设置：保持默认「仅创建者可读写」（写入走云函数服务端，绕过客户端权限规则，无需改权限）。
   - 集合中会自动由 `saveSchedule` 云函数写入一条 `doc('current')` 记录（结构：`{ _id:'current', schedule:{ '日期':'时段数组' }, updatedAt }`），无需手动建记录。

2. **确认 `bookings` 集合的冲突检测索引**
   - 进入 `bookings` 集合 →「索引管理」。
   - 确认存在索引（计划中预留）：
     - 索引名：`idx_visitDate_time`
     - 字段：`visitDate`(升序) + `visitTime`(升序)
     - 若无，手动「新建索引」添加（用于 `checkSlot` 高效查占用）。
   - 权限：需为「**所有用户可读**」（普通用户要查全量占用），写入走云函数。

---

## 三、真机验证（代码+部署都完成后）

用**两部手机**或「真机预览 + 另一账号」模拟两个用户：

### A. 操作师侧（管理员）
1. 进入「时段管理」页。
2. 选某日期 → 默认三个时段应全部显示 **「关闭」**。
3. 点开其中 1~2 个时段 → 保存。
4. 确认云开发控制台 `schedule` 集合 `doc('current')` 已写入对应数据。

### B. 普通用户侧
1. 选同一日期 → 只有操作师开启的时段显示 **「可约」**；未开启显示 **「未开放」**（灰显）。
2. 选一个「可约」时段提交预约（需先微信授权手机号）→ 成功后该时段对所有用户变为 **「已约满」**。
3. **另一用户**选同一日期同一时段 → 状态列表标「已约满」；若硬选提交 → 提示「该时段已被预约」并拦截。
4. 操作师在后台把该预约**取消/标记失约** → 该时段自动恢复 **「可约」**。

### C. 降级说明
- **微信开发者工具（演示模式）**：跳过云端，`schedule` 读本地 `_schedule`。可在本地 storage 写入 `{'日期':['9:30-11:30']}` 验证三态 UI 与拦截逻辑，但**跨用户效果必须用真机验证**。
- **云函数未部署 / 调用失败**：`checkSlot` 走 catch 降级信任客户端判断（仍能拦本地已加载的占用），但并发双占防护会减弱——所以**务必完成第一步部署**。

---

## 四、常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| 顾客端时段全显示「未开放」 | 操作师没开启时段 / `schedule` 集合没数据 | 操作师开启并保存；确认 `saveSchedule` 已部署 |
| 提交时报 `checkSlot` 调用失败 | 云函数未上传部署 | 回到「一」重新上传部署 |
| 两个用户仍抢到同一时段 | `checkSlot` 未部署或失效 | 确认云函数已部署；必要时升级为事务版 `reserveSlot` |
| 开发者工具里看不到云端效果 | 演示模式跳过云端 | 正常现象，用真机预览验证 |

---

最后更新：2026-08-21
